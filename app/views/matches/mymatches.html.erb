<h1>Listing your matches</h1>

<p>inline buttons with above header</p>

<%= render 'uploader' %>
<%= render 'match_finder' %>

<%= render 'filter_form' %>

<% if @matches.length > 0 %>
  <%= render 'matchlist' %>
<% else %>
  <h2>You haven't uploaded or requested any matches</h2>
<% end %>
